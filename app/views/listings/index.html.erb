<h2>Home</h2>	

<ul>
	<% @list.each do |listing| %>
		<li><%= listing.name %><br/>
		
		<%= listing.description %><br/>
		

        <% if listing.images != nil %>
			
			<% listing.images.each do |individual| %>

				<img src= "<%= individual.thumb.url %>" >		
			<% end %>	
			<%= form_tag("/listings/show/nav/#{listing.id}", method: :post) do %>
    			<input type="submit" value="Interested?" />
    		<% end %>
    		<%#= link_to "New Reservation", new_user_listing_reservation_path([current_user, listing]) %>
		<% end %>

	</li>
		<br/><br/>
	<% end %>
</ul>

 